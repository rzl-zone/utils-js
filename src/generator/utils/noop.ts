/** --------------------------------------------------
 * * ***This method returns `undefined`.***
 * --------------------------------------------------
 *
 * @example
 * console.log(noop) // => undefined
 * isUndefined(noop) // => true
 *
 */
export const noop = () => {};
